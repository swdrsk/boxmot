# PreloadSORT アルゴリズム詳細解説

## 1. はじめに

**PreloadSORT**は、追跡対象が事前にわかっている（*a priori*）シナリオ向けに設計された、新しい多物体追跡（MOT）アルゴリズムです。検出されたすべての物体を追跡する一般的なトラッカー（BotSORT、DeepSORTなど）とは異なり、PreloadSORTは事前に登録された参照画像データベースに基づいて検出をフィルタリングします。

このアプローチは、ターゲットが頻繁にフレームアウト・フレームインするシナリオ（例：ステージパフォーマンス、スポーツ）における「IDスイッチング」問題を効果的に解決し、セッション全体を通して特定の個人が一意のIDを維持できるようにします。

## 2. コアコンセプト

PreloadSORTの中核となる哲学は **「Filter First, Track Later（まずフィルタリング、その後追跡）」** です。

1.  **登録フェーズ**: ターゲットの参照画像から深層特徴量（Deep Feature Embeddings）を抽出し、保存します。
2.  **推論フェーズ**:
    *   毎フレーム、すべての物体を検出します。
    *   各検出を登録済みデータベースと比較します。
    *   登録済みターゲットと高い信頼度で一致しない検出は **破棄（Discard）** します。
    *   「検証済み」の検出のみを追跡エンジン（カルマンフィルタ + ハンガリアン法）に渡します。

## 3. アルゴリズム詳細

### 3.1. 登録（初期化）

$N$ 人の登録人物セット $\mathcal{R} = \{P_1, P_2, ..., P_N\}$ を定義します。
各人物 $P_i$ について、参照画像セット $\mathcal{I}_i = \{I_{i,1}, I_{i,2}, ..., I_{i,K}\}$ を持ちます。

ReID（再識別）モデル $\Phi$ を用いて各画像のFeature Embedding $f_{i,k}$ を抽出します：

$$
f_{i,k} = \Phi(I_{i,k}), \quad f_{i,k} \in \mathbb{R}^d, \quad \|f_{i,k}\|_2 = 1
$$

すべての埋め込みはデータベース $\mathcal{D}$ に保存されます：
$$
\mathcal{D} = \{ (i, f_{i,k}) \mid i \in \{1..N\}, k \in \{1..K\} \}
$$

> **Note**: 特徴量を1つのプロトタイプに平均化する手法とは異なり、PreloadSORTは **Winner-Take-All** 戦略ですべての埋め込みを保持し、姿勢や照明の大きな変動に自然に対応します。

---

### 3.2. 検出と特徴抽出（毎フレーム）

フレーム $t$ において、物体検出器（YOLOなど）から検出セット $\mathcal{B}_t = \{b_1, b_2, ..., b_M\}$ を取得します（$b_j$ はバウンディングボックス）。

各バウンディングボックス $b_j$ について、外観埋め込み $e_j$ を抽出します：

$$
e_j = \Phi(\text{Crop}(Frame_t, b_j)), \quad \|e_j\|_2 = 1
$$

### 3.3. プレフィルタリング（"Preload" ステップ）

各検出埋め込み $e_j$ と、$\mathcal{D}$ 内のすべての登録済み埋め込みとの類似度を計算します。ここではコサイン類似度（正規化ベクトル同士の内積）を使用します。

検出 $j$ の最大類似度スコア $S_j$ は以下の通りです：

$$
S_j = \max_{(i, f) \in \mathcal{D}} (e_j \cdot f)
$$

マッチしたID $\hat{ID}_j$ は：

$$
\hat{ID}_j = \underset{i}{\text{argmax}} \left( \max_{f \in \mathcal{D}_i} (e_j \cdot f) \right)
$$

**フィルタリングロジック**:
$$
\text{Keep } b_j \iff S_j \geq \tau_{match}
$$
ここで $\tau_{match}$ はユーザー定義のマッチング閾値（例: 0.45）です。この閾値を下回る検出は即座に破棄されます。

---

### 3.4. トラックアソシエーション

PreloadSORTはBotSORTに基づく多段階アソシエーションを採用していますが、未知の物体を追跡しないように簡略化されています。

現在アクティブなトラックセット $\mathcal{T}_{active}$ を、カルマンフィルタ（KF）を用いてフレーム $t$ の位置へ予測します。

#### コスト行列の構築
有効な検出とアクティブなトラック間のコスト行列 $C$ を計算します。コストは **IoU (Intersection over Union)** と **ReID埋め込み距離** を組み合わせたものです。

1.  **IoU距離**: $D_{iou} = 1 - \text{IoU}(Box_{track}, Box_{det})$
2.  **埋め込み距離**: $D_{emb} = 1 - (e_{track} \cdot e_{det})$

融合コスト $C_{ij}$ は以下のように定義されます：

$$
C_{ij} = \min(D_{iou}(i, j), D_{emb}(i, j))
$$

*※ $D_{iou}$ が近接閾値を超える場合、コストは無限大とします。*

#### 線形割り当て（Linear Assignment）
ハンガリアン法（LAP）を用いて、トラックと検出間の最適な割り当てを解きます。

#### 未マッチトラックの処理
*   標準的なトラッカー（ByteTrack/BotSORT）では、未マッチトラックに対して低信頼度検出を用いた「第2段階アソシエーション」を行いますが、**PreloadSORTではこれをスキップします**。
*   *理由*: 有効なターゲットは高い意味的マッチングスコアを持つはずであるためです。低信頼度の検出やゴミ検出と無理に紐付けることは、背景ノイズを拾うリスクになります。

---

### 3.5. 新規トラックの初期化

検出 $b_j$ が既存のトラックとマッチしない場合、新規トラックの作成を検討します。

**制約**: 以下の条件をすべて満たす場合のみ、新規トラックが作成されます。
1.  検出が登録IDとマッチしていること（$\hat{ID}_j \neq \text{None}$）。
2.  そのIDを持つアクティブなトラックが現在存在しないこと。

これにより、ReIDの精度限界で別人が同一IDにマッピングされない限り、「1つの登録IDにつき1つのトラック」というルールを厳格に適用します。

---

### 3.6. Ghost Killing（ゴースト除去）

ReIDモデルが背景テクスチャ（例：人物のポスターや壁の模様）を誤って自信満々にマッチングしてしまう「偽陽性」に対処するため、PreloadSORTは **Ghost Track Suppression（ゴーストトラック抑制）** メカニズムを搭載しています。

各トラック $T$ について、その移動ベクトル $\Delta \mathbf{x}$ を監視します。

$$
\text{Ghost}(T) = 
\begin{cases} 
True & \text{if } \sum_{t=now-W}^{now} \mathbb{I}(\|\Delta \mathbf{x}_t\| < \epsilon_{move}) > \tau_{static} \\
False & \text{otherwise}
\end{cases}
$$

*   $\epsilon_{move}$: 移動閾値（ピクセル単位）。
*   $\tau_{static}$: 静止時間閾値（フレーム数）。

トラックが「Ghost（幽霊）」と判定された場合、即座に削除（強制終了）されます。

---

## 4. BotSORTとの比較

| 機能 | BotSORT | PreloadSORT (本アルゴリズム) |
| :--- | :--- | :--- |
| **追跡範囲** | 検出器が見つけたすべての物体 | **事前登録されたターゲットのみ** |
| **ID割り当て** | 出現順の連番 (1, 2, 3...) | **登録IDに固定** (ID 5は常にPerson 5) |
| **再入場時の挙動** | 新規IDを割り当て (ID 1退場→ID 5入場) | **オリジナルIDを復元** (ID 5退場→ID 5入場) |
| **低信頼度検出** | 第2段階アソシエーションに使用 | **無視** (純度を保つため) |
| **Ghost対策** | なし (検出器任せ) | **Ghost Killer** 実装済み (静止誤検出を除去) |

## 5. サマリーフローチャート

```mermaid
graph TD
    A[入力フレーム] --> B[YOLO検出]
    B --> C{各検出について}
    C -->|特徴抽出| D[ReIDモデル]
    D --> E[登録DBと比較]
    E --> F{類似度 > 閾値?}
    F -- No --> G[検出を破棄]
    F -- Yes --> H[検出を保持 & ID割当]
    H --> I[カルマンフィルタ予測]
    I --> J[アソシエーション (IoU + ReID)]
    J --> K{マッチした?}
    K -- Yes --> L[カルマンフィルタ更新]
    K -- No --> M{ID空きあり?}
    M -- Yes --> N[新規トラック初期化]
    M -- No --> O[破棄 (重複)]
    L --> P[Ghostチェック (移動量)]
    P -->|静止| Q[トラック強制削除]
    P -->|移動中| R[トラック出力]
```
